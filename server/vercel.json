{
  "builds": [
    {
      "src": "./index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/component-login/(.*)", "dest": "/" },
    { "src": "/project-marriage-ste/(.*)", "dest": "/" }
  ]
}