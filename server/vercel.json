{
  "builds": [
    {
      "src": "./index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/login-component/(.*)", "dest": "/" },
    { "src": "/image-recognition/(.*)", "dest": "/" }
  ]
}